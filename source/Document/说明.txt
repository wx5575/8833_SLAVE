由于字库取出的μΩ是宽字符，所以不能直接使用，现在使用窄字符~代替μ（esc下方的键） 
Ω需要调整字模调整后的字模保存在μΩ.txt文件中?

uint32_t cpu_sr;
OS_ENTER_CRITICAL();//关中断
OS_EXIT_CRITICAL();//开中断

开调度器
关闭调度

注意：取字模时应注意宋体小四加粗字体取出来的大小是17*17而软件却把它当成16*16的来处理，这应该是取模软件的一个BUG，为了正常显示应将字模中的字长和自宽改为16 确实是很无语

SimSun_16.c 生成的字库做了修改备份文件在SimSun_16.c.bak中
